<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.numad21su_osmansubasi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_os"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_os_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SuOsmanSubasi">
        <activity android:name=".LinkCollectorActivity"></activity>
        <activity android:name=".PressMeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>